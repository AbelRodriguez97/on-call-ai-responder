version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: on-call-ai-responder-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    restart: always